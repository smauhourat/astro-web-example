---
const response = await fetch("https://jsonplaceholder.typicode.com/todos");
const posts = await response.json();
//console.log(data);
---

<ul>
  {
    posts.map((post) => (
      <li>
        <h1>{post.title}</h1>
      </li>
    ))
  }
</ul>
